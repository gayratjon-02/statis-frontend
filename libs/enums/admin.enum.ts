// =============================================
// ENUMS — Admin (mirrors backend AdminRole)
// =============================================

/** Admin role types */
export enum AdminRole {
    SUPER_ADMIN = "super_admin",
    CONTENT_ADMIN = "content_admin",
    SUPPORT = "support",
}
