services:
  static-engine-frontend:
    container_name: static-frontend
    build:
      context: .
      args:
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://167.172.90.235:4009}
        NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL:-ws://167.172.90.235:4009}
    restart: always
    env_file:
      - .env
    ports:
      - "4010:5010"
    environment:
      NODE_ENV: production
      PORT: 5010
      HOSTNAME: "0.0.0.0"
    networks:
      - frontend-network

networks:
  frontend-network:
    driver: bridge
